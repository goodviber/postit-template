<h3>Welcome!</h3>

<p>This is the Postit template. You've access the PostsController, index action. This is the index template. Find me under app/views/posts folder.</p>

 <div class="well">
	<table>
	  <tr>
	  	<th>Category</th>
	    <th>Url</th>
	    <th>Title</th>
	    <th>Text</th>
	    <th colspan="6"></th>
	  </tr>

	  
	 
	  <% @posts.each do |post| %>
	    <tr>
				<td>
				  <% post.categories.each do |category|  %>
				    <%= category.name %>
				  <% end %>
				</td>
	      <td><%= link_to fix_url(post.url) %></td>
	      <td><%= post.title %></td>
	      <td><%= post.description %></td>
	      <td> <%= link_to 'Show', post %>&nbsp</td>
	      <% if logged_in? %>
	      <td> <%= link_to 'Edit', edit_post_path(post) %>&nbsp</td>
	      <% end %>
	      <td>posted by <%= post.user.username %> on <%= pretty_date(post.created_at) %></td>
	     <!-- <td><%= link_to 'Destroy', post_path(post),
	                    method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
	    </tr>
  	<% end %>
	</table>

	
</div>
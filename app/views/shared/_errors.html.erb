<% if obj.errors.any? %>
  <div class="row">
    <div class="alert alert-error span8">
      <div id="error_explanation">
        <h4><%= pluralize(obj.errors.count, "error") %> prohibited
            this post from being saved:</h4>
        <ul>
          <% obj.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
<% end %>